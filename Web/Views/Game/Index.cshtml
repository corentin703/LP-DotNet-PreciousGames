@using Web.Models.GameModels;
@model IndexModel

@{
    ViewBag.Title = "GameIndex";
}

<h2>GameIndex</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayName(nameof(GameViewModel.Name))
        </th>
        <th>
            @Html.DisplayName(nameof(GameViewModel.Description))
        </th>
        <th>
            @Html.DisplayName(nameof(GameViewModel.ReleaseDate))
        </th>
        <th>
            @Html.DisplayName(nameof(GameViewModel.Kind))
        </th>
        <th>
            @Html.DisplayName(nameof(GameViewModel.Editor))
        </th>
        <th></th>
    </tr>

@foreach (var item in Model.Games) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Description)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ReleaseDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Kind.Id)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Editor.Id)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
        </td>
    </tr>
}

</table>
