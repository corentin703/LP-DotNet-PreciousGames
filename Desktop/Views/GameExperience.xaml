<UserControl x:Class="VerotMorin.PreciousGames.Desktop.Views.GameExperience"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewModels="clr-namespace:VerotMorin.PreciousGames.Desktop.ViewModels"
             d:DataContext="{d:DesignInstance Type=viewModels:GameExperienceViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <TextBox Text="{Binding Player}"></TextBox>
        <TextBox Text="{Binding PlayedTime}"></TextBox>
        <TextBox Text="{Binding Percentage}"></TextBox>
    </Grid>
</UserControl>
